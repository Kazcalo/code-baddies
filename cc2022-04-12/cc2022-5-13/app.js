// Implement a function which multiplies two numbers

let multiply = (6 ,9 ) ; Math.imul (6,9)

// square root or not sqare root
const squareOrSquareRoot = array => array.map(a => Math.sqrt(a) % 1 == 0 ? Math.sqrt(a) : (a * a));
